version: 2

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    - go generate ./...

archives:
  - ids:
      - ws
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: ["tar.gz"]
    files:
      - README.md
      - LICENSE*

builds:
  - binary: ws
    id: ws
    ldflags:
      - -s -w -X github.com/zmoog/ws/v2/commands.version={{.Version}} -X github.com/zmoog/ws/v2/commands.commit={{.Commit}} -X github.com/zmoog/ws/v2/commands.date={{.Date}} -X github.com/zmoog/ws/v2/commands.builtBy=goreleaser
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./main.go

release:
  name_template: "{{.ProjectName}} v{{.Version}}"
  draft: false
  prerelease: auto

homebrew_casks:
  - name: ws
    repository:
      owner: zmoog
      name: homebrew-ws
      branch: main
    binaries:
      - ws
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/ws"]
          end
checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"